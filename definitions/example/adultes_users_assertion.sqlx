config {
    type: "assertion",
    tags: [constants.assertion_label],
}

SELECT
  "user not adulte" AS failing_row_condition,
  *
FROM
  ${ref("stg_users")}
WHERE
  DATE_DIFF(CURRENT_DATE(),birth_date, YEAR)<18
